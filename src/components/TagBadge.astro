---
import { slug } from "github-slugger";
import { withBase } from "../lib/site";

interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a class="tag" href={withBase(`/tags/${slug(tag)}/`)}>{tag}</a>
